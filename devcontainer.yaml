# See https://containers.dev/ for more information.
# This is the official Dev Container configuration for Java development (with the blue checkmark badge).
version: "1.0"

name: "Java"

# Use the official Microsoft Java Dev Container image
image: "mcr.microsoft.com/devcontainers/java:0-17"

features:
  # Add the official Desktop Lite feature if needed
  ghcr.io/devcontainers/features/desktop-lite:1: {}

customizations:
  vscode:
    extensions:
      - vscjava.vscode-java-pack
